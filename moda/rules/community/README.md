# Community YARA Rules

Use this directory for locally curated community rules that are safe to keep in
the project repository.

For large upstream repositories, prefer `rules/external/` and pin the upstream
commit in `rules/external_sources.yml`.
